// ────────────────────────────────────────────────────────────────
// 사업자 · 연락처 정보 (단일 관리 지점)
//
// 이 파일의 값만 고치면 아래 화면 전체에 한 번에 반영됩니다.
//   · 푸터 (src/components/Footer.js)
//   · 개인정보처리방침 / 이용약관 / 운영자 소개 / 요금 / 연락처 페이지
//
// 비워 둔 항목('')은 화면에서 그 줄이 자동으로 빠집니다.
// (가짜 값이 노출되지 않도록, 채우기 전까지는 표시하지 않는 방식입니다.)
// ────────────────────────────────────────────────────────────────

export const BUSINESS = {
  businessName: '와이제이인터내셔널',
  operator: '전영재',

  // 사업자등록번호 — 실제 값 그대로 유지합니다.
  businessRegNo: '569-10-02302',

  // TODO(주소): 공개 가능한 사업장 주소를 채워 주세요.
  //   · 자택 주소 / 아파트 동호수는 넣지 마세요. 비상주 사무실·사서함 주소 권장.
  //   · 예: '경기도 파주시 ○○로 12, 3층 301호'
  address: '',

  // TODO(전화): 고객 문의용 대표 번호를 채워 주세요.
  //   · 개인 휴대폰 대신 070 인터넷전화 / 안심번호 권장.
  //   · 예: '070-0000-0000'
  phone: '',

  // 문의 이메일 — 기존에 공개돼 있던 주소를 그대로 둡니다.
  // 대표 메일(예: 'help@도메인')이 생기면 이 값만 바꾸면 전체에 반영됩니다.
  email: 'joun90@gmail.com',
}

// 채워진 항목만 ' · '로 이어 붙인 사업자 정보 한 줄.
export function businessLine() {
  return [
    `${BUSINESS.businessName}(대표: ${BUSINESS.operator})`,
    BUSINESS.businessRegNo && `사업자등록번호 ${BUSINESS.businessRegNo}`,
    BUSINESS.address,
    BUSINESS.phone,
  ].filter(Boolean).join(' · ')
}
