{
  "name": "saju-unse",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy --only functions,hosting:sajuunse"
  },
  "dependencies": {
    "@orrery/core": "^0.1.0",
    "@tosspayments/tosspayments-sdk": "^2.7.1",
    "firebase": "^12.17.1"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
